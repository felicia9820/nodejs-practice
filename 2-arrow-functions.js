const square = function(x) {
    return x * x
}

const square = (x) => {
    return x * x
}

const square = (x) => x * x

const event = {
    name: 'Birthday',
    printGuests() {
        console.log('Guest list for ' + this.name)
    }
}